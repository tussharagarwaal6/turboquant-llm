Use the browser to:

1. Open http://localhost:8000/docs
2. Find GET `/v1/models` in the Swagger UI
3. Expand it, click "Try it out", then "Execute"
4. Screenshot the response body
5. Report whether `Qwen/Qwen3-14B-AWQ` appears in the JSON

Pass criteria: HTTP 200 and model id present in response.
