Open http://localhost:8000/docs and confirm the FastAPI Swagger UI loads.

1. Navigate to http://localhost:8000/docs
2. Verify the page title or header mentions Swagger or OpenAPI
3. Return PASS if the docs page loaded, else FAIL with the error you see
